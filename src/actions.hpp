#include <netinet/in.h>             // sockaddr_in
#include <string>
#include <vector>


#include "package.hpp"

namespace action
{
    void authen_response_action(const package::authentication_response &ar);

    
}